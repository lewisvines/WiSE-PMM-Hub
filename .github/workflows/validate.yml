name: Validate hub data
on:
  push:
    paths: ['data/**']
  pull_request:
    paths: ['data/**']
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate data integrity
        run: python3 scripts/validate_data.py
